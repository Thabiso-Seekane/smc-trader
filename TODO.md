# STEP 9 â€” Validator â€” Implementation TODO

- [x] Confirm plan with user
- [x] Create `core/exceptions.py` with `SMCError` base and `DataValidationError`
- [x] Create `data/validator.py` with `validate_candles(frame)` and all required checks
- [x] Update `core/__init__.py` to export `DataValidationError` / `SMCError`
- [x] Create `tests/test_validator.py` covering every validation rule
- [x] Run `python -m pytest tests/test_validator.py -v`
- [x] Run full test suite `python -m pytest -v`


# STEP 10 — Storage — Implementation TODO
- [x] Confirm plan with user
- [ ] Create  data`/storage.py` with save_candles and load_candles`n- [ ] Update data/__init__.py` to export storage helpers
- [ ] Create 	ests/test_storage.py` with storage coverage
- [ ] Run python -m pytest tests/test_storage.py -v`n- [ ] Run full test suite python -m pytest -v`
